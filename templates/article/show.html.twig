{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <div class="header d-flex justify-content-between">
        <h1>{{ article.title}}</h1>
        <a href="{{ path('app_article_index') }}" class="btn btn-sm btn-outline-primary" margin-left="60%" height="25%">Retour à la liste</a>
        <a href="{{ path('app_article_edit', {'slug': article.slug}) }}" class="btn btn-sm btn-outline-primary" margin-left="60%" height="25%">Editer</a>
        {{ include('article/_delete_form.html.twig') }}
    </div>
    <div class="column1 d-flex flex-column" width:"50%">
        <img src="{{ article.photo}}" width="250" height="250"/> 
        <br>
        <p class="text-justify">{{article.content}}</p>
    </div>
    <div class="column2 d-flex flex-column" width:"50%">
    </div>
    
{% endblock %}
